Quickstart

1. Copy `.env.example` to `.env` and add `OPENAI_API_KEY`.
2. Run `npm install`.
3. Run `npm run dev -- web-search "your query"`.

If you want real web results, add Bing or SerpAPI and update `WebSearchCommand`.
